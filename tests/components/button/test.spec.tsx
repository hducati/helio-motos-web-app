import { screen } from '@testing-library/react'

import Button from 'components/button/button'

describe('<Button />', () => {
  it('should be able to render the medium size by defualt', () => {})
})
